import accelerateable from './accelerateable';
import animateable from './animateable';
import inputControlable from './inputControlable';
import moveable from './moveable';
import positionable from './positionable';
import spriteRenderable from './spriteRenderable';

const componentFns = {
  accelerateable,
  animateable,
  inputControlable,
  moveable,
  positionable,
  spriteRenderable,
};

export default componentFns;
