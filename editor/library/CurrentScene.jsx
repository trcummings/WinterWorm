// @flow
import React, { PureComponent } from 'react';

export default class CurrentScene extends PureComponent { // eslint-disable-line
  render() {
    return (
      <div>
        list of all scenes
        on select, refresh game with that scene
      </div>
    );
  }
}
